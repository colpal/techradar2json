{
  "scripts": {
    "format": "prettier --write",
    "test": "vitest run",
    "watch": "npm run build -- --watch=forever",
    "build": "esbuild main.ts --bundle --platform=node --outfile=techradar2json"
  },
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "@types/node": "^22.13.11",
    "esbuild": "^0.25.1",
    "eslint": "^9.22.0",
    "globals": "^16.0.0",
    "prettier": "^3.5.3",
    "typescript-eslint": "^8.27.0",
    "vitest": "^3.0.9"
  },
  "dependencies": {
    "yaml": "^2.7.0"
  },
  "volta": {
    "node": "22.14.0"
  },
  "version": "1.0.0"
}
